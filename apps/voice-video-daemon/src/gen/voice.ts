// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.180.0
//   protoc               v4.25.1
// source: voice.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Observable } from "rxjs";
import { map } from "rxjs/operators";

export const protobufPackage = "mycat.voice";

export interface ClientMessage {
  audio?: AudioChunk | undefined;
  control?: Control | undefined;
}

export interface AudioChunk {
  /** PCM 16-bit LE or Opus frame */
  data: Uint8Array;
  /** e.g., 16000 */
  sampleRate: number;
  /** true if data is opus-encoded */
  isOpus: boolean;
  /** VAD boundary */
  endOfUtterance: boolean;
}

export interface Control {
  sessionId: string;
  /** client is holding the talk key */
  pushToTalk: boolean;
  /** request graceful stop */
  stop: boolean;
}

export interface ServerMessage {
  sessionId: string;
  asr?: AsrResult | undefined;
  llm?: LlmUpdate | undefined;
  action?: ActionEvent | undefined;
  tts?: TtsChunk | undefined;
  health?: Health | undefined;
}

export interface AsrResult {
  text: string;
  isFinal: boolean;
  latencyMs: number;
}

export interface LlmUpdate {
  /** streaming tokens */
  partialText: string;
  isFinal: boolean;
}

export interface ActionEvent {
  id: string;
  /** browser|desktop|approval */
  type: string;
  title: string;
  detail: string;
  /** low|medium|high */
  riskLevel: string;
  approvalRequired: boolean;
  /** planned|approved|rejected|running|done|error */
  status: string;
}

export interface TtsChunk {
  /** PCM/Opus chunk */
  audio: Uint8Array;
  isOpus: boolean;
  isFinal: boolean;
}

export interface Health {
  /** ready|degraded|error */
  state: string;
  message: string;
}

function createBaseClientMessage(): ClientMessage {
  return { audio: undefined, control: undefined };
}

export const ClientMessage = {
  encode(message: ClientMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.audio !== undefined) {
      AudioChunk.encode(message.audio, writer.uint32(10).fork()).ldelim();
    }
    if (message.control !== undefined) {
      Control.encode(message.control, writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ClientMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.audio = AudioChunk.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.control = Control.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientMessage {
    return {
      audio: isSet(object.audio) ? AudioChunk.fromJSON(object.audio) : undefined,
      control: isSet(object.control) ? Control.fromJSON(object.control) : undefined,
    };
  },

  toJSON(message: ClientMessage): unknown {
    const obj: any = {};
    if (message.audio !== undefined) {
      obj.audio = AudioChunk.toJSON(message.audio);
    }
    if (message.control !== undefined) {
      obj.control = Control.toJSON(message.control);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientMessage>, I>>(base?: I): ClientMessage {
    return ClientMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientMessage>, I>>(object: I): ClientMessage {
    const message = createBaseClientMessage();
    message.audio = (object.audio !== undefined && object.audio !== null)
      ? AudioChunk.fromPartial(object.audio)
      : undefined;
    message.control = (object.control !== undefined && object.control !== null)
      ? Control.fromPartial(object.control)
      : undefined;
    return message;
  },
};

function createBaseAudioChunk(): AudioChunk {
  return { data: new Uint8Array(0), sampleRate: 0, isOpus: false, endOfUtterance: false };
}

export const AudioChunk = {
  encode(message: AudioChunk, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.data.length !== 0) {
      writer.uint32(10).bytes(message.data);
    }
    if (message.sampleRate !== 0) {
      writer.uint32(16).uint32(message.sampleRate);
    }
    if (message.isOpus !== false) {
      writer.uint32(24).bool(message.isOpus);
    }
    if (message.endOfUtterance !== false) {
      writer.uint32(32).bool(message.endOfUtterance);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AudioChunk {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAudioChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.data = reader.bytes();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.sampleRate = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isOpus = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.endOfUtterance = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AudioChunk {
    return {
      data: isSet(object.data) ? bytesFromBase64(object.data) : new Uint8Array(0),
      sampleRate: isSet(object.sampleRate) ? globalThis.Number(object.sampleRate) : 0,
      isOpus: isSet(object.isOpus) ? globalThis.Boolean(object.isOpus) : false,
      endOfUtterance: isSet(object.endOfUtterance) ? globalThis.Boolean(object.endOfUtterance) : false,
    };
  },

  toJSON(message: AudioChunk): unknown {
    const obj: any = {};
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.sampleRate !== 0) {
      obj.sampleRate = Math.round(message.sampleRate);
    }
    if (message.isOpus !== false) {
      obj.isOpus = message.isOpus;
    }
    if (message.endOfUtterance !== false) {
      obj.endOfUtterance = message.endOfUtterance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AudioChunk>, I>>(base?: I): AudioChunk {
    return AudioChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AudioChunk>, I>>(object: I): AudioChunk {
    const message = createBaseAudioChunk();
    message.data = object.data ?? new Uint8Array(0);
    message.sampleRate = object.sampleRate ?? 0;
    message.isOpus = object.isOpus ?? false;
    message.endOfUtterance = object.endOfUtterance ?? false;
    return message;
  },
};

function createBaseControl(): Control {
  return { sessionId: "", pushToTalk: false, stop: false };
}

export const Control = {
  encode(message: Control, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.pushToTalk !== false) {
      writer.uint32(16).bool(message.pushToTalk);
    }
    if (message.stop !== false) {
      writer.uint32(24).bool(message.stop);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Control {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseControl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.pushToTalk = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.stop = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Control {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      pushToTalk: isSet(object.pushToTalk) ? globalThis.Boolean(object.pushToTalk) : false,
      stop: isSet(object.stop) ? globalThis.Boolean(object.stop) : false,
    };
  },

  toJSON(message: Control): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.pushToTalk !== false) {
      obj.pushToTalk = message.pushToTalk;
    }
    if (message.stop !== false) {
      obj.stop = message.stop;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Control>, I>>(base?: I): Control {
    return Control.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Control>, I>>(object: I): Control {
    const message = createBaseControl();
    message.sessionId = object.sessionId ?? "";
    message.pushToTalk = object.pushToTalk ?? false;
    message.stop = object.stop ?? false;
    return message;
  },
};

function createBaseServerMessage(): ServerMessage {
  return { sessionId: "", asr: undefined, llm: undefined, action: undefined, tts: undefined, health: undefined };
}

export const ServerMessage = {
  encode(message: ServerMessage, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.asr !== undefined) {
      AsrResult.encode(message.asr, writer.uint32(18).fork()).ldelim();
    }
    if (message.llm !== undefined) {
      LlmUpdate.encode(message.llm, writer.uint32(26).fork()).ldelim();
    }
    if (message.action !== undefined) {
      ActionEvent.encode(message.action, writer.uint32(34).fork()).ldelim();
    }
    if (message.tts !== undefined) {
      TtsChunk.encode(message.tts, writer.uint32(42).fork()).ldelim();
    }
    if (message.health !== undefined) {
      Health.encode(message.health, writer.uint32(50).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ServerMessage {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.asr = AsrResult.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.llm = LlmUpdate.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.action = ActionEvent.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.tts = TtsChunk.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.health = Health.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerMessage {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      asr: isSet(object.asr) ? AsrResult.fromJSON(object.asr) : undefined,
      llm: isSet(object.llm) ? LlmUpdate.fromJSON(object.llm) : undefined,
      action: isSet(object.action) ? ActionEvent.fromJSON(object.action) : undefined,
      tts: isSet(object.tts) ? TtsChunk.fromJSON(object.tts) : undefined,
      health: isSet(object.health) ? Health.fromJSON(object.health) : undefined,
    };
  },

  toJSON(message: ServerMessage): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.asr !== undefined) {
      obj.asr = AsrResult.toJSON(message.asr);
    }
    if (message.llm !== undefined) {
      obj.llm = LlmUpdate.toJSON(message.llm);
    }
    if (message.action !== undefined) {
      obj.action = ActionEvent.toJSON(message.action);
    }
    if (message.tts !== undefined) {
      obj.tts = TtsChunk.toJSON(message.tts);
    }
    if (message.health !== undefined) {
      obj.health = Health.toJSON(message.health);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerMessage>, I>>(base?: I): ServerMessage {
    return ServerMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerMessage>, I>>(object: I): ServerMessage {
    const message = createBaseServerMessage();
    message.sessionId = object.sessionId ?? "";
    message.asr = (object.asr !== undefined && object.asr !== null) ? AsrResult.fromPartial(object.asr) : undefined;
    message.llm = (object.llm !== undefined && object.llm !== null) ? LlmUpdate.fromPartial(object.llm) : undefined;
    message.action = (object.action !== undefined && object.action !== null)
      ? ActionEvent.fromPartial(object.action)
      : undefined;
    message.tts = (object.tts !== undefined && object.tts !== null) ? TtsChunk.fromPartial(object.tts) : undefined;
    message.health = (object.health !== undefined && object.health !== null)
      ? Health.fromPartial(object.health)
      : undefined;
    return message;
  },
};

function createBaseAsrResult(): AsrResult {
  return { text: "", isFinal: false, latencyMs: 0 };
}

export const AsrResult = {
  encode(message: AsrResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    if (message.latencyMs !== 0) {
      writer.uint32(25).double(message.latencyMs);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): AsrResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAsrResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.text = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
        case 3:
          if (tag !== 25) {
            break;
          }

          message.latencyMs = reader.double();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AsrResult {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      isFinal: isSet(object.isFinal) ? globalThis.Boolean(object.isFinal) : false,
      latencyMs: isSet(object.latencyMs) ? globalThis.Number(object.latencyMs) : 0,
    };
  },

  toJSON(message: AsrResult): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    if (message.latencyMs !== 0) {
      obj.latencyMs = message.latencyMs;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AsrResult>, I>>(base?: I): AsrResult {
    return AsrResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AsrResult>, I>>(object: I): AsrResult {
    const message = createBaseAsrResult();
    message.text = object.text ?? "";
    message.isFinal = object.isFinal ?? false;
    message.latencyMs = object.latencyMs ?? 0;
    return message;
  },
};

function createBaseLlmUpdate(): LlmUpdate {
  return { partialText: "", isFinal: false };
}

export const LlmUpdate = {
  encode(message: LlmUpdate, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.partialText !== "") {
      writer.uint32(10).string(message.partialText);
    }
    if (message.isFinal !== false) {
      writer.uint32(16).bool(message.isFinal);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): LlmUpdate {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLlmUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.partialText = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LlmUpdate {
    return {
      partialText: isSet(object.partialText) ? globalThis.String(object.partialText) : "",
      isFinal: isSet(object.isFinal) ? globalThis.Boolean(object.isFinal) : false,
    };
  },

  toJSON(message: LlmUpdate): unknown {
    const obj: any = {};
    if (message.partialText !== "") {
      obj.partialText = message.partialText;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LlmUpdate>, I>>(base?: I): LlmUpdate {
    return LlmUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LlmUpdate>, I>>(object: I): LlmUpdate {
    const message = createBaseLlmUpdate();
    message.partialText = object.partialText ?? "";
    message.isFinal = object.isFinal ?? false;
    return message;
  },
};

function createBaseActionEvent(): ActionEvent {
  return { id: "", type: "", title: "", detail: "", riskLevel: "", approvalRequired: false, status: "" };
}

export const ActionEvent = {
  encode(message: ActionEvent, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.type !== "") {
      writer.uint32(18).string(message.type);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.detail !== "") {
      writer.uint32(34).string(message.detail);
    }
    if (message.riskLevel !== "") {
      writer.uint32(42).string(message.riskLevel);
    }
    if (message.approvalRequired !== false) {
      writer.uint32(48).bool(message.approvalRequired);
    }
    if (message.status !== "") {
      writer.uint32(58).string(message.status);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ActionEvent {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseActionEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.type = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.detail = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.riskLevel = reader.string();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.approvalRequired = reader.bool();
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.status = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ActionEvent {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
      riskLevel: isSet(object.riskLevel) ? globalThis.String(object.riskLevel) : "",
      approvalRequired: isSet(object.approvalRequired) ? globalThis.Boolean(object.approvalRequired) : false,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: ActionEvent): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    if (message.riskLevel !== "") {
      obj.riskLevel = message.riskLevel;
    }
    if (message.approvalRequired !== false) {
      obj.approvalRequired = message.approvalRequired;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ActionEvent>, I>>(base?: I): ActionEvent {
    return ActionEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ActionEvent>, I>>(object: I): ActionEvent {
    const message = createBaseActionEvent();
    message.id = object.id ?? "";
    message.type = object.type ?? "";
    message.title = object.title ?? "";
    message.detail = object.detail ?? "";
    message.riskLevel = object.riskLevel ?? "";
    message.approvalRequired = object.approvalRequired ?? false;
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseTtsChunk(): TtsChunk {
  return { audio: new Uint8Array(0), isOpus: false, isFinal: false };
}

export const TtsChunk = {
  encode(message: TtsChunk, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.audio.length !== 0) {
      writer.uint32(10).bytes(message.audio);
    }
    if (message.isOpus !== false) {
      writer.uint32(16).bool(message.isOpus);
    }
    if (message.isFinal !== false) {
      writer.uint32(24).bool(message.isFinal);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): TtsChunk {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTtsChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.audio = reader.bytes();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.isOpus = reader.bool();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isFinal = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TtsChunk {
    return {
      audio: isSet(object.audio) ? bytesFromBase64(object.audio) : new Uint8Array(0),
      isOpus: isSet(object.isOpus) ? globalThis.Boolean(object.isOpus) : false,
      isFinal: isSet(object.isFinal) ? globalThis.Boolean(object.isFinal) : false,
    };
  },

  toJSON(message: TtsChunk): unknown {
    const obj: any = {};
    if (message.audio.length !== 0) {
      obj.audio = base64FromBytes(message.audio);
    }
    if (message.isOpus !== false) {
      obj.isOpus = message.isOpus;
    }
    if (message.isFinal !== false) {
      obj.isFinal = message.isFinal;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TtsChunk>, I>>(base?: I): TtsChunk {
    return TtsChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TtsChunk>, I>>(object: I): TtsChunk {
    const message = createBaseTtsChunk();
    message.audio = object.audio ?? new Uint8Array(0);
    message.isOpus = object.isOpus ?? false;
    message.isFinal = object.isFinal ?? false;
    return message;
  },
};

function createBaseHealth(): Health {
  return { state: "", message: "" };
}

export const Health = {
  encode(message: Health, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.state !== "") {
      writer.uint32(10).string(message.state);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Health {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.state = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Health {
    return {
      state: isSet(object.state) ? globalThis.String(object.state) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: Health): unknown {
    const obj: any = {};
    if (message.state !== "") {
      obj.state = message.state;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Health>, I>>(base?: I): Health {
    return Health.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Health>, I>>(object: I): Health {
    const message = createBaseHealth();
    message.state = object.state ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

/** Bidirectional audio/LLM/TTS stream for macOS-first assistant. */
export interface VoiceStream {
  /**
   * Start a full-duplex session; client sends AudioChunk / Control messages,
   * server returns ASR transcripts, LLM drafts, action plans, and TTS audio.
   */
  Stream(request: Observable<ClientMessage>): Observable<ServerMessage>;
}

export const VoiceStreamServiceName = "mycat.voice.VoiceStream";
export class VoiceStreamClientImpl implements VoiceStream {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || VoiceStreamServiceName;
    this.rpc = rpc;
    this.Stream = this.Stream.bind(this);
  }
  Stream(request: Observable<ClientMessage>): Observable<ServerMessage> {
    const data = request.pipe(map((request) => ClientMessage.encode(request).finish()));
    const result = this.rpc.bidirectionalStreamingRequest(this.service, "Stream", data);
    return result.pipe(map((data) => ServerMessage.decode(_m0.Reader.create(data))));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
  clientStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Promise<Uint8Array>;
  serverStreamingRequest(service: string, method: string, data: Uint8Array): Observable<Uint8Array>;
  bidirectionalStreamingRequest(service: string, method: string, data: Observable<Uint8Array>): Observable<Uint8Array>;
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
